@model IEnumerable<CrawWebAssignment.Models.Content>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UrlSource)
        </th>
        <th>

        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <a href="@(item.UrlSource ?? "")">@(item.UrlSource ?? "")</a>

                @*@Html.DisplayFor(modelItem => item.Url)*@
            </td>
        </tr>
    }

</table>